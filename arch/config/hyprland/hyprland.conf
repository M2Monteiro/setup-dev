source = ~/.config/hypr/macchiato.conf

### MONITORS ###
$monitorL = DP-1
$monitorR = DP-2
monitor=$monitorL, preferred, 0x0, 1 # Left
monitor=$monitorR, 1366x768, 1920x0, 1 # Right

### SYSTEM = DARK MODE ###
exec = gsettings set org.gnome.desktop.interface gtk-theme "adw-gtk3-dark" # for GTK3
exec = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"   # for GTK4 apps
env = QT_QPA_PLATFORMTHEME,qt6ct   # for Qt apps

### MY PROGRAMS ###
$terminal = kitty
$fileManager = dolphin
$menu = wofi --show drun
$emojiPicker = wofi-emoji

$browser = zen-browser
$discordClient = vesktop

#################
### AUTOSTART ###
#################
exec-once = hyprpaper & $terminal & waybar
exec-once = [workspace 6 silent] $discordClient


### ENVIRONMENT VARIABLES ###
# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = HYPRCURSOR_THEME,macchiato-dark
env = HYPRCURSOR_SIZE,24
env = MOZ_ENABLE_WAYLAND,1
env = XDG_MENU_PREFIX,arch-

### PERMISSIONS ###
# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

cursor {
    inactive_timeout = 3
    no_hardware_cursors = true
}

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 1
    gaps_out = 2
    border_size = 1
    
    

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.inactive_border = rgba($sapphireAlpha4d)
    col.active_border = rgba($blueAlphaee)

    layout = dwindle

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 0
    rounding_power = 4.0

    shadow {
        enabled = false
    }
    
    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 8
        passes = 3

        popups = true
        new_optimizations = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 8, almostLinear, fade
    animation = workspacesIn, 1, 2, easeInOutCubic, slide
    animation = workspacesOut, 1, 2, easeInOutCubic, slide
}

#workspace = w[tv1], gapsout:0, gapsin:0
#workspace = f[1], gapsout:0, gapsin:0
#windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
#windowrule = rounding 0, floating:0, onworkspace:w[tv1]
#windowrule = bordersize 0, floating:0, onworkspace:f[1]
#windowrule = rounding 0, floating:0, onworkspace:f[1]


# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
## WORKSPACE / MONITOR ##
workspace = 1, monitor:$monitorL, persistent:true
workspace = 2, monitor:$monitorL, persistent:true
workspace = 3, monitor:$monitorL, persistent:true
workspace = 4, monitor:$monitorL, persistent:true
workspace = 5, monitor:$monitorL, persistent:true
workspace = 6, monitor:$monitorR, persistent:true
workspace = 7, monitor:$monitorR, persistent:true
workspace = 8, monitor:$monitorR, persistent:true
workspace = 9, monitor:$monitorR, persistent:true
workspace = 10, monitor:$monitorR, persistent:true

# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    animate_manual_resizes = false
}


#############
### INPUT ###
#############

env=CUSTOM_ACCEL_MULTIPLIER,1.7

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us,us
    kb_variant = ,intl        
    kb_options = grp:win_space_toggle,deadkeys:acute
    kb_model =
    kb_rules =

    accel_profile = flat
    follow_mouse = 1
    sensitivity = 0.7 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################


# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier
$fnMod = Fn

### Hyprshot ###
env = HYPRSHOT_DIR,$HOME/screenshots
# Screenshot a region
bind = , PRINT, exec, hyprshot -m region 
# Screenshot a window
bind = $shiftMod, PRINT, exec, hyprshot -m window 
# Screenshot a monitor
bind = $mainMod, PRINT, exec, hyprshot -m output

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, T, exec, $terminal
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, B, exec, $browser
bind = $mainMod, C, killactive,
bind = $mainMod, Q, exec, ~/scripts/forcekill.sh,
bind = $mainMod, Escape, exit,
bind = $mainMod, V, togglefloating,
bind = $mainMod, P, exec, $menu
bind = $mainMod, O, exec, $emojiPicker
bind = $mainMod, R, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, F, fullscreen,

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Toggle waybar when Super key is pressed
bindr = $mainMod, TAB, exec, killall -SIGUSR1 waybar

# Move between adjacent workspaces 
bind = $mainMod CTRL, right, workspace, m+1
bind = $mainMod CTRL, left, workspace, m-1

# Move windows between adjacent workspaces 
bind = $mainMod CTRL SHIFT, right, movetoworkspace, m+1
bind = $mainMod CTRL SHIFT, left, movetoworkspace, m-1

# Move windows between monitors
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, down, movewindow, d
bind = $mainMod SHIFT, up, movewindow, u

# Resize with keyboard
bind = $mainMod ALT, right, resizeactive, 80 0
bind = $mainMod ALT, left, resizeactive, -80 0
bind = $mainMod ALT, down, resizeactive, 0 80
bind = $mainMod ALT, up, resizeactive, 0 -80

# Volume controls
bind = Control_R, up, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind = Control_R, down, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%

# Example special workspace (scratchpad)
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Opacity
windowrule = opacity 0.9, match:class ^(Spotify|$discordClient|Postman)$
# windowrule = opacity 0,9, class:zen
windowrule = opacity 0.9, match:class code
# windowrulev2 = noblur, class:^(code)$
# windowrulev2 = noborder, onworkspace:w[t1]
# windowrulev2 = norounding, onworkspace:w[t1]

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppress_event maximize, match:class .*

# Fix some dragging issues with XWayland
windowrule = no_focus on, match:class ^$, match:title ^$, match:xwayland 1, match:float 1, match:fullscreen 0, match:pin 0


debug {
    full_cm_proto=true
}


